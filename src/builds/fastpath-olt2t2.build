#!/bin/sh

export MODULE=fastpath-olt2t2
export PLATFORMS="CXO2T2 TA12XG TT08SXG"

export APPLICATION=fp

#echo "Updating SVN..."
#svn update

echo "Compiling fastpath for OLT1T3..."
make -C ../fastpath -f swdrvr-olt2t2.make all
if [ $? -ne 0 ]; then
 echo ERROR!!!
 exit 1;
fi
echo "Copying binaries to apps..."
make -C ../fastpath -f swdrvr-olt2t2.make install
if [ $? -ne 0 ]; then
 echo ERROR!!!
 exit 1;
fi
echo "Done... Going to prepare tarball."
echo

. ./fastpath.build $1
