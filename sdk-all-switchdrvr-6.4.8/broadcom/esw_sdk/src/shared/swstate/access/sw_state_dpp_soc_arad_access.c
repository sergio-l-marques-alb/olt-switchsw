/* $Id: $
 * 
 * $Copyright: Copyright 2015 Broadcom Corporation.
 * This program is the proprietary software of Broadcom Corporation
 * and/or its licensors, and may only be used, duplicated, modified
 * or distributed pursuant to the terms and conditions of a separate,
 * written license agreement executed between you and Broadcom
 * (an "Authorized License").  Except as set forth in an Authorized
 * License, Broadcom grants no license (express or implied), right
 * to use, or waiver of any kind with respect to the Software, and
 * Broadcom expressly reserves all rights in and to the Software
 * and all intellectual property rights therein.  IF YOU HAVE
 * NO AUTHORIZED LICENSE, THEN YOU HAVE NO RIGHT TO USE THIS SOFTWARE
 * IN ANY WAY, AND SHOULD IMMEDIATELY NOTIFY BROADCOM AND DISCONTINUE
 * ALL USE OF THE SOFTWARE.  
 *  
 * Except as expressly set forth in the Authorized License,
 *  
 * 1.     This program, including its structure, sequence and organization,
 * constitutes the valuable trade secrets of Broadcom, and you shall use
 * all reasonable efforts to protect the confidentiality thereof,
 * and to use this information only in connection with your use of
 * Broadcom integrated circuit products.
 *  
 * 2.     TO THE MAXIMUM EXTENT PERMITTED BY LAW, THE SOFTWARE IS
 * PROVIDED "AS IS" AND WITH ALL FAULTS AND BROADCOM MAKES NO PROMISES,
 * REPRESENTATIONS OR WARRANTIES, EITHER EXPRESS, IMPLIED, STATUTORY,
 * OR OTHERWISE, WITH RESPECT TO THE SOFTWARE.  BROADCOM SPECIFICALLY
 * DISCLAIMS ANY AND ALL IMPLIED WARRANTIES OF TITLE, MERCHANTABILITY,
 * NONINFRINGEMENT, FITNESS FOR A PARTICULAR PURPOSE, LACK OF VIRUSES,
 * ACCURACY OR COMPLETENESS, QUIET ENJOYMENT, QUIET POSSESSION OR
 * CORRESPONDENCE TO DESCRIPTION. YOU ASSUME THE ENTIRE RISK ARISING
 * OUT OF USE OR PERFORMANCE OF THE SOFTWARE.
 * 
 * 3.     TO THE MAXIMUM EXTENT PERMITTED BY LAW, IN NO EVENT SHALL
 * BROADCOM OR ITS LICENSORS BE LIABLE FOR (i) CONSEQUENTIAL,
 * INCIDENTAL, SPECIAL, INDIRECT, OR EXEMPLARY DAMAGES WHATSOEVER
 * ARISING OUT OF OR IN ANY WAY RELATING TO YOUR USE OF OR INABILITY
 * TO USE THE SOFTWARE EVEN IF BROADCOM HAS BEEN ADVISED OF THE
 * POSSIBILITY OF SUCH DAMAGES; OR (ii) ANY AMOUNT IN EXCESS OF
 * THE AMOUNT ACTUALLY PAID FOR THE SOFTWARE ITSELF OR USD 1.00,
 * WHICHEVER IS GREATER. THESE LIMITATIONS SHALL APPLY NOTWITHSTANDING
 * ANY FAILURE OF ESSENTIAL PURPOSE OF ANY LIMITED REMEDY.$
 * 
 * DO NOT EDIT THIS FILE!
 * This file is auto-generated.
 * Edits to this file will be lost when it is regenerated.
 */

#include <sal/core/libc.h>
#include <shared/swstate/sw_state.h>
#include <shared/error.h>
#include <shared/swstate/sw_state_defs.h>
#include <shared/bsl.h>
#include <shared/swstate/access/sw_state_access.h>
#include <shared/swstate/access/sw_state_dpp_soc_arad_access.h>


/* AUTO-GENERATED - DO NOT MODIFY */
int sw_state_dpp_soc_arad_verify(int _unit){
    int rv = _SHR_E_NONE;
    rv = sw_state_access[_unit].dpp.soc.verify( _unit);
    if (rv != _SHR_E_NONE) return rv;
    return rv;
}


/* AUTO-GENERATED - DO NOT MODIFY */
int sw_state_dpp_soc_arad_get_size(int _unit, int *ret_size){
    int rv = _SHR_E_NONE;
    int size = 0;
    INIT_INDEXES();
    size = size + sizeof(sw_state[_unit]->dpp.soc.arad);
    
    is_param_allocated = (sw_state[_unit]->dpp.soc.arad.tm) ? 1 : 0;
    if (is_param_allocated) {
    /* need to reduce the size of sw_state[_unit]->dpp.soc.arad.tm because  it's included in the 
        above and we are going recalculate it. */
        size = size - sizeof(sw_state[_unit]->dpp.soc.arad.tm);
        sw_state_access[_unit].dpp.soc.arad.tm.get_size (  _unit, &temp_size);
        size = size + temp_size;
    }
    
    
    is_param_allocated = (sw_state[_unit]->dpp.soc.arad.pp) ? 1 : 0;
    if (is_param_allocated) {
    /* need to reduce the size of sw_state[_unit]->dpp.soc.arad.pp because  it's included in the 
        above and we are going recalculate it. */
        size = size - sizeof(sw_state[_unit]->dpp.soc.arad.pp);
        sw_state_access[_unit].dpp.soc.arad.pp.get_size (  _unit, &temp_size);
        size = size + temp_size;
    }
    
    *ret_size = size;
    return rv;
}


/* AUTO-GENERATED - DO NOT MODIFY */
int sw_state_dpp_soc_arad_tm_alloc(int _unit){
    int rv = _SHR_E_NONE;
    SW_STATE_IS_WARM_BOOT_CHECK();
    SW_STATE_IS_DEINIT_CHECK();
    SW_STATE_IS_ALREADY_ALLOCATED_CHECK(sw_state[_unit]->dpp.soc.arad.tm);
    rv = shr_sw_state_alloc(_unit, (uint8 **) &sw_state[_unit]->dpp.soc.arad.tm, sizeof(soc_arad_sw_state_tm_t));
    return rv;
}

/* AUTO-GENERATED - DO NOT MODIFY */
int sw_state_dpp_soc_arad_tm_is_allocated(int _unit, uint8 *is_allocated){
    int rv = _SHR_E_NONE;
    *is_allocated = (sw_state[_unit]->dpp.soc.arad.tm) ? 1 : 0;
    rv = _SHR_E_NONE;
    return rv;
}

/* AUTO-GENERATED - DO NOT MODIFY */
int sw_state_dpp_soc_arad_tm_free(int _unit){
    int rv = _SHR_E_NONE;
    SW_STATE_IS_WARM_BOOT_CHECK();
    SW_STATE_IS_DEINIT_CHECK();
    SW_STATE_NULL_CHECK(sw_state[_unit]->dpp.soc.arad.tm);
    rv = shr_sw_state_free(_unit, (uint8 *) sw_state[_unit]->dpp.soc.arad.tm);
    return rv;
}


/* AUTO-GENERATED - DO NOT MODIFY */
int sw_state_dpp_soc_arad_tm_verify(int _unit){
    int rv = _SHR_E_NONE;
    rv = sw_state_access[_unit].dpp.soc.arad.verify( _unit);
    if (rv != _SHR_E_NONE) return rv;
    SW_STATE_NULL_ACCESS_CHECK(sw_state[_unit]->dpp.soc.arad.tm);
    return rv;
}


/* AUTO-GENERATED - DO NOT MODIFY */
int sw_state_dpp_soc_arad_tm_get_size(int _unit, int *ret_size){
    int rv = _SHR_E_NONE;
    int size = 0;
    INIT_INDEXES();
    size = size + sizeof(sw_state[_unit]->dpp.soc.arad.tm);
    size = size + sizeof(*sw_state[_unit]->dpp.soc.arad.tm);
    
    is_param_allocated = (sw_state[_unit]->dpp.soc.arad.tm->op_mode) ? 1 : 0;
    if (is_param_allocated) {
    /* need to reduce the size of sw_state[_unit]->dpp.soc.arad.tm->op_mode because  it's included in the 
        above and we are going recalculate it. */
        size = size - sizeof(sw_state[_unit]->dpp.soc.arad.tm->op_mode);
        sw_state_access[_unit].dpp.soc.arad.tm.op_mode.get_size (  _unit, &temp_size);
        size = size + temp_size;
    }
    
    
    is_param_allocated = (sw_state[_unit]->dpp.soc.arad.tm->jer_modid_group_map) ? 1 : 0;
    if (is_param_allocated) {
    /* need to reduce the size of sw_state[_unit]->dpp.soc.arad.tm->jer_modid_group_map because  it's included in the 
        above and we are going recalculate it. */
        size = size - sizeof(sw_state[_unit]->dpp.soc.arad.tm->jer_modid_group_map);
        sw_state_access[_unit].dpp.soc.arad.tm.jer_modid_group_map.get_size (  _unit, &temp_size);
        size = size + temp_size;
    }
    
    
    is_param_allocated = (sw_state[_unit]->dpp.soc.arad.tm->arad_plus) ? 1 : 0;
    if (is_param_allocated) {
    /* need to reduce the size of sw_state[_unit]->dpp.soc.arad.tm->arad_plus because  it's included in the 
        above and we are going recalculate it. */
        size = size - sizeof(sw_state[_unit]->dpp.soc.arad.tm->arad_plus);
        sw_state_access[_unit].dpp.soc.arad.tm.arad_plus.get_size (  _unit, &temp_size);
        size = size + temp_size;
    }
    
    
    is_param_allocated = (sw_state[_unit]->dpp.soc.arad.tm->tm_info) ? 1 : 0;
    if (is_param_allocated) {
    /* need to reduce the size of sw_state[_unit]->dpp.soc.arad.tm->tm_info because  it's included in the 
        above and we are going recalculate it. */
        size = size - sizeof(sw_state[_unit]->dpp.soc.arad.tm->tm_info);
        sw_state_access[_unit].dpp.soc.arad.tm.tm_info.get_size (  _unit, &temp_size);
        size = size + temp_size;
    }
    
    
    is_param_allocated = (sw_state[_unit]->dpp.soc.arad.tm->tcam_mgmt) ? 1 : 0;
    if (is_param_allocated) {
    /* need to reduce the size of sw_state[_unit]->dpp.soc.arad.tm->tcam_mgmt because  it's included in the 
        above and we are going recalculate it. */
        size = size - sizeof(sw_state[_unit]->dpp.soc.arad.tm->tcam_mgmt);
        sw_state_access[_unit].dpp.soc.arad.tm.tcam_mgmt.get_size (  _unit, &temp_size);
        size = size + temp_size;
    }
    
    #ifdef LINK_ARAD_LIBRARIES

    is_param_allocated = (sw_state[_unit]->dpp.soc.arad.tm->tcam) ? 1 : 0;
    if (is_param_allocated) {
    /* need to reduce the size of sw_state[_unit]->dpp.soc.arad.tm->tcam because  it's included in the 
        above and we are going recalculate it. */
        size = size - sizeof(sw_state[_unit]->dpp.soc.arad.tm->tcam);
        sw_state_access[_unit].dpp.soc.arad.tm.tcam.get_size (  _unit, &temp_size);
        size = size + temp_size;
    }
    #endif /* LINK_ARAD_LIBRARIES*/ 

    
    is_param_allocated = (sw_state[_unit]->dpp.soc.arad.tm->pmf) ? 1 : 0;
    if (is_param_allocated) {
        shr_sw_state_allocated_size_get (_unit, (uint8*)sw_state[_unit]->dpp.soc.arad.tm->pmf, &allocated_size);
        toIdx0 = allocated_size /
             sizeof(*(sw_state[_unit]->dpp.soc.arad.tm->pmf));
        for (idx0 = 0; idx0 < toIdx0; idx0++) {
            sw_state_access[_unit].dpp.soc.arad.tm.pmf.get_size (  _unit, idx0, &temp_size);
            size = size + temp_size;
        }
    }
    
    
    is_param_allocated = (sw_state[_unit]->dpp.soc.arad.tm->arad_egr_ports) ? 1 : 0;
    if (is_param_allocated) {
    /* need to reduce the size of sw_state[_unit]->dpp.soc.arad.tm->arad_egr_ports because  it's included in the 
        above and we are going recalculate it. */
        size = size - sizeof(sw_state[_unit]->dpp.soc.arad.tm->arad_egr_ports);
        sw_state_access[_unit].dpp.soc.arad.tm.arad_egr_ports.get_size (  _unit, &temp_size);
        size = size + temp_size;
    }
    
    #if defined(INCLUDE_KBP)&&!defined(BCM_88030)

    is_param_allocated = (sw_state[_unit]->dpp.soc.arad.tm->kbp_info) ? 1 : 0;
    if (is_param_allocated) {
    /* need to reduce the size of sw_state[_unit]->dpp.soc.arad.tm->kbp_info because  it's included in the 
        above and we are going recalculate it. */
        size = size - sizeof(sw_state[_unit]->dpp.soc.arad.tm->kbp_info);
        sw_state_access[_unit].dpp.soc.arad.tm.kbp_info.get_size (  _unit, &temp_size);
        size = size + temp_size;
    }
    #endif /* defined(INCLUDE_KBP)&&!defined(BCM_88030)*/ 

    
    is_param_allocated = (sw_state[_unit]->dpp.soc.arad.tm->lag) ? 1 : 0;
    if (is_param_allocated) {
    /* need to reduce the size of sw_state[_unit]->dpp.soc.arad.tm->lag because  it's included in the 
        above and we are going recalculate it. */
        size = size - sizeof(sw_state[_unit]->dpp.soc.arad.tm->lag);
        sw_state_access[_unit].dpp.soc.arad.tm.lag.get_size (  _unit, &temp_size);
        size = size + temp_size;
    }
    
    
    is_param_allocated = (sw_state[_unit]->dpp.soc.arad.tm->cell) ? 1 : 0;
    if (is_param_allocated) {
    /* need to reduce the size of sw_state[_unit]->dpp.soc.arad.tm->cell because  it's included in the 
        above and we are going recalculate it. */
        size = size - sizeof(sw_state[_unit]->dpp.soc.arad.tm->cell);
        sw_state_access[_unit].dpp.soc.arad.tm.cell.get_size (  _unit, &temp_size);
        size = size + temp_size;
    }
    
    
    is_param_allocated = (sw_state[_unit]->dpp.soc.arad.tm->tdm) ? 1 : 0;
    if (is_param_allocated) {
    /* need to reduce the size of sw_state[_unit]->dpp.soc.arad.tm->tdm because  it's included in the 
        above and we are going recalculate it. */
        size = size - sizeof(sw_state[_unit]->dpp.soc.arad.tm->tdm);
        sw_state_access[_unit].dpp.soc.arad.tm.tdm.get_size (  _unit, &temp_size);
        size = size + temp_size;
    }
    
    
    is_param_allocated = (sw_state[_unit]->dpp.soc.arad.tm->dram) ? 1 : 0;
    if (is_param_allocated) {
    /* need to reduce the size of sw_state[_unit]->dpp.soc.arad.tm->dram because  it's included in the 
        above and we are going recalculate it. */
        size = size - sizeof(sw_state[_unit]->dpp.soc.arad.tm->dram);
        sw_state_access[_unit].dpp.soc.arad.tm.dram.get_size (  _unit, &temp_size);
        size = size + temp_size;
    }
    
    
    is_param_allocated = (sw_state[_unit]->dpp.soc.arad.tm->arad_multicast) ? 1 : 0;
    if (is_param_allocated) {
    /* need to reduce the size of sw_state[_unit]->dpp.soc.arad.tm->arad_multicast because  it's included in the 
        above and we are going recalculate it. */
        size = size - sizeof(sw_state[_unit]->dpp.soc.arad.tm->arad_multicast);
        sw_state_access[_unit].dpp.soc.arad.tm.arad_multicast.get_size (  _unit, &temp_size);
        size = size + temp_size;
    }
    
    
    is_param_allocated = (sw_state[_unit]->dpp.soc.arad.tm->q_type_ref_count) ? 1 : 0;
    if (is_param_allocated) {
        shr_sw_state_allocated_size_get (_unit, (uint8*)sw_state[_unit]->dpp.soc.arad.tm->q_type_ref_count, &allocated_size);
        toIdx0 = allocated_size /
             sizeof(*(sw_state[_unit]->dpp.soc.arad.tm->q_type_ref_count));
        for (idx0 = 0; idx0 < toIdx0; idx0++) {
            sw_state_access[_unit].dpp.soc.arad.tm.q_type_ref_count.get_size (  _unit, idx0, &temp_size);
            size = size + temp_size;
        }
    }
    
    
    is_param_allocated = (sw_state[_unit]->dpp.soc.arad.tm->q_type_map) ? 1 : 0;
    if (is_param_allocated) {
        shr_sw_state_allocated_size_get (_unit, (uint8*)sw_state[_unit]->dpp.soc.arad.tm->q_type_map, &allocated_size);
        toIdx0 = allocated_size /
             sizeof(*(sw_state[_unit]->dpp.soc.arad.tm->q_type_map));
        for (idx0 = 0; idx0 < toIdx0; idx0++) {
            sw_state_access[_unit].dpp.soc.arad.tm.q_type_map.get_size (  _unit, idx0, &temp_size);
            size = size + temp_size;
        }
    }
    
    
    is_param_allocated = (sw_state[_unit]->dpp.soc.arad.tm->vsi) ? 1 : 0;
    if (is_param_allocated) {
    /* need to reduce the size of sw_state[_unit]->dpp.soc.arad.tm->vsi because  it's included in the 
        above and we are going recalculate it. */
        size = size - sizeof(sw_state[_unit]->dpp.soc.arad.tm->vsi);
        sw_state_access[_unit].dpp.soc.arad.tm.vsi.get_size (  _unit, &temp_size);
        size = size + temp_size;
    }
    
    
    is_param_allocated = (sw_state[_unit]->dpp.soc.arad.tm->modport2sysport) ? 1 : 0;
    if (is_param_allocated) {
        shr_sw_state_allocated_size_get (_unit, (uint8*)sw_state[_unit]->dpp.soc.arad.tm->modport2sysport, &allocated_size);
        toIdx0 = allocated_size /
             sizeof(*(sw_state[_unit]->dpp.soc.arad.tm->modport2sysport));
        for (idx0 = 0; idx0 < toIdx0; idx0++) {
            sw_state_access[_unit].dpp.soc.arad.tm.modport2sysport.get_size (  _unit, idx0, &temp_size);
            size = size + temp_size;
        }
    }
    
    
    is_param_allocated = (sw_state[_unit]->dpp.soc.arad.tm->interrupts) ? 1 : 0;
    if (is_param_allocated) {
    /* need to reduce the size of sw_state[_unit]->dpp.soc.arad.tm->interrupts because  it's included in the 
        above and we are going recalculate it. */
        size = size - sizeof(sw_state[_unit]->dpp.soc.arad.tm->interrupts);
        sw_state_access[_unit].dpp.soc.arad.tm.interrupts.get_size (  _unit, &temp_size);
        size = size + temp_size;
    }
    
    
    is_param_allocated = (sw_state[_unit]->dpp.soc.arad.tm->chip_definitions) ? 1 : 0;
    if (is_param_allocated) {
    /* need to reduce the size of sw_state[_unit]->dpp.soc.arad.tm->chip_definitions because  it's included in the 
        above and we are going recalculate it. */
        size = size - sizeof(sw_state[_unit]->dpp.soc.arad.tm->chip_definitions);
        sw_state_access[_unit].dpp.soc.arad.tm.chip_definitions.get_size (  _unit, &temp_size);
        size = size + temp_size;
    }
    
    
    is_param_allocated = (sw_state[_unit]->dpp.soc.arad.tm->phy_ports_info) ? 1 : 0;
    if (is_param_allocated) {
        shr_sw_state_allocated_size_get (_unit, (uint8*)sw_state[_unit]->dpp.soc.arad.tm->phy_ports_info, &allocated_size);
        toIdx0 = allocated_size /
             sizeof(*(sw_state[_unit]->dpp.soc.arad.tm->phy_ports_info));
        for (idx0 = 0; idx0 < toIdx0; idx0++) {
            sw_state_access[_unit].dpp.soc.arad.tm.phy_ports_info.get_size (  _unit, idx0, &temp_size);
            size = size + temp_size;
        }
    }
    
    
    is_param_allocated = (sw_state[_unit]->dpp.soc.arad.tm->logical_ports_info) ? 1 : 0;
    if (is_param_allocated) {
        shr_sw_state_allocated_size_get (_unit, (uint8*)sw_state[_unit]->dpp.soc.arad.tm->logical_ports_info, &allocated_size);
        toIdx0 = allocated_size /
             sizeof(*(sw_state[_unit]->dpp.soc.arad.tm->logical_ports_info));
        for (idx0 = 0; idx0 < toIdx0; idx0++) {
            sw_state_access[_unit].dpp.soc.arad.tm.logical_ports_info.get_size (  _unit, idx0, &temp_size);
            size = size + temp_size;
        }
    }
    
    
    is_param_allocated = (sw_state[_unit]->dpp.soc.arad.tm->reassembly_ctxt) ? 1 : 0;
    if (is_param_allocated) {
    /* need to reduce the size of sw_state[_unit]->dpp.soc.arad.tm->reassembly_ctxt because  it's included in the 
        above and we are going recalculate it. */
        size = size - sizeof(sw_state[_unit]->dpp.soc.arad.tm->reassembly_ctxt);
        sw_state_access[_unit].dpp.soc.arad.tm.reassembly_ctxt.get_size (  _unit, &temp_size);
        size = size + temp_size;
    }
    
    
    is_param_allocated = (sw_state[_unit]->dpp.soc.arad.tm->arad_em_blocks) ? 1 : 0;
    if (is_param_allocated) {
        shr_sw_state_allocated_size_get (_unit, (uint8*)sw_state[_unit]->dpp.soc.arad.tm->arad_em_blocks, &allocated_size);
        toIdx0 = allocated_size /
             sizeof(*(sw_state[_unit]->dpp.soc.arad.tm->arad_em_blocks));
        for (idx0 = 0; idx0 < toIdx0; idx0++) {
            sw_state_access[_unit].dpp.soc.arad.tm.arad_em_blocks.get_size (  _unit, idx0, &temp_size);
            size = size + temp_size;
        }
    }
    
    
    is_param_allocated = (sw_state[_unit]->dpp.soc.arad.tm->guaranteed_q_resource) ? 1 : 0;
    if (is_param_allocated) {
        shr_sw_state_allocated_size_get (_unit, (uint8*)sw_state[_unit]->dpp.soc.arad.tm->guaranteed_q_resource, &allocated_size);
        toIdx0 = allocated_size /
             sizeof(*(sw_state[_unit]->dpp.soc.arad.tm->guaranteed_q_resource));
        for (idx0 = 0; idx0 < toIdx0; idx0++) {
            sw_state_access[_unit].dpp.soc.arad.tm.guaranteed_q_resource.get_size (  _unit, idx0, &temp_size);
            size = size + temp_size;
        }
    }
    
    
    is_param_allocated = (sw_state[_unit]->dpp.soc.arad.tm->egr_prog_editor) ? 1 : 0;
    if (is_param_allocated) {
    /* need to reduce the size of sw_state[_unit]->dpp.soc.arad.tm->egr_prog_editor because  it's included in the 
        above and we are going recalculate it. */
        size = size - sizeof(sw_state[_unit]->dpp.soc.arad.tm->egr_prog_editor);
        sw_state_access[_unit].dpp.soc.arad.tm.egr_prog_editor.get_size (  _unit, &temp_size);
        size = size + temp_size;
    }
    
    *ret_size = size;
    return rv;
}


/* AUTO-GENERATED - DO NOT MODIFY */
int sw_state_dpp_soc_arad_pp_alloc(int _unit){
    int rv = _SHR_E_NONE;
    SW_STATE_IS_WARM_BOOT_CHECK();
    SW_STATE_IS_DEINIT_CHECK();
    SW_STATE_IS_ALREADY_ALLOCATED_CHECK(sw_state[_unit]->dpp.soc.arad.pp);
    rv = shr_sw_state_alloc(_unit, (uint8 **) &sw_state[_unit]->dpp.soc.arad.pp, sizeof(soc_arad_sw_state_pp_t));
    return rv;
}

/* AUTO-GENERATED - DO NOT MODIFY */
int sw_state_dpp_soc_arad_pp_is_allocated(int _unit, uint8 *is_allocated){
    int rv = _SHR_E_NONE;
    *is_allocated = (sw_state[_unit]->dpp.soc.arad.pp) ? 1 : 0;
    rv = _SHR_E_NONE;
    return rv;
}

/* AUTO-GENERATED - DO NOT MODIFY */
int sw_state_dpp_soc_arad_pp_free(int _unit){
    int rv = _SHR_E_NONE;
    SW_STATE_IS_WARM_BOOT_CHECK();
    SW_STATE_IS_DEINIT_CHECK();
    SW_STATE_NULL_CHECK(sw_state[_unit]->dpp.soc.arad.pp);
    rv = shr_sw_state_free(_unit, (uint8 *) sw_state[_unit]->dpp.soc.arad.pp);
    return rv;
}


/* AUTO-GENERATED - DO NOT MODIFY */
int sw_state_dpp_soc_arad_pp_verify(int _unit){
    int rv = _SHR_E_NONE;
    rv = sw_state_access[_unit].dpp.soc.arad.verify( _unit);
    if (rv != _SHR_E_NONE) return rv;
    SW_STATE_NULL_ACCESS_CHECK(sw_state[_unit]->dpp.soc.arad.pp);
    return rv;
}


/* AUTO-GENERATED - DO NOT MODIFY */
int sw_state_dpp_soc_arad_pp_get_size(int _unit, int *ret_size){
    int rv = _SHR_E_NONE;
    int size = 0;
    INIT_INDEXES();
    size = size + sizeof(sw_state[_unit]->dpp.soc.arad.pp);
    size = size + sizeof(*sw_state[_unit]->dpp.soc.arad.pp);
    
    is_param_allocated = (sw_state[_unit]->dpp.soc.arad.pp->ecmp_info) ? 1 : 0;
    if (is_param_allocated) {
    /* need to reduce the size of sw_state[_unit]->dpp.soc.arad.pp->ecmp_info because  it's included in the 
        above and we are going recalculate it. */
        size = size - sizeof(sw_state[_unit]->dpp.soc.arad.pp->ecmp_info);
        sw_state_access[_unit].dpp.soc.arad.pp.ecmp_info.get_size (  _unit, &temp_size);
        size = size + temp_size;
    }
    
    
    is_param_allocated = (sw_state[_unit]->dpp.soc.arad.pp->dbal_info) ? 1 : 0;
    if (is_param_allocated) {
    /* need to reduce the size of sw_state[_unit]->dpp.soc.arad.pp->dbal_info because  it's included in the 
        above and we are going recalculate it. */
        size = size - sizeof(sw_state[_unit]->dpp.soc.arad.pp->dbal_info);
        sw_state_access[_unit].dpp.soc.arad.pp.dbal_info.get_size (  _unit, &temp_size);
        size = size + temp_size;
    }
    
    
    is_param_allocated = (sw_state[_unit]->dpp.soc.arad.pp->llp_vid_assign) ? 1 : 0;
    if (is_param_allocated) {
    /* need to reduce the size of sw_state[_unit]->dpp.soc.arad.pp->llp_vid_assign because  it's included in the 
        above and we are going recalculate it. */
        size = size - sizeof(sw_state[_unit]->dpp.soc.arad.pp->llp_vid_assign);
        sw_state_access[_unit].dpp.soc.arad.pp.llp_vid_assign.get_size (  _unit, &temp_size);
        size = size + temp_size;
    }
    
    
    is_param_allocated = (sw_state[_unit]->dpp.soc.arad.pp->llp_filter) ? 1 : 0;
    if (is_param_allocated) {
    /* need to reduce the size of sw_state[_unit]->dpp.soc.arad.pp->llp_filter because  it's included in the 
        above and we are going recalculate it. */
        size = size - sizeof(sw_state[_unit]->dpp.soc.arad.pp->llp_filter);
        sw_state_access[_unit].dpp.soc.arad.pp.llp_filter.get_size (  _unit, &temp_size);
        size = size + temp_size;
    }
    
    
    is_param_allocated = (sw_state[_unit]->dpp.soc.arad.pp->pon_double_lookup) ? 1 : 0;
    if (is_param_allocated) {
    /* need to reduce the size of sw_state[_unit]->dpp.soc.arad.pp->pon_double_lookup because  it's included in the 
        above and we are going recalculate it. */
        size = size - sizeof(sw_state[_unit]->dpp.soc.arad.pp->pon_double_lookup);
        sw_state_access[_unit].dpp.soc.arad.pp.pon_double_lookup.get_size (  _unit, &temp_size);
        size = size + temp_size;
    }
    
    
    is_param_allocated = (sw_state[_unit]->dpp.soc.arad.pp->vrrp_info) ? 1 : 0;
    if (is_param_allocated) {
    /* need to reduce the size of sw_state[_unit]->dpp.soc.arad.pp->vrrp_info because  it's included in the 
        above and we are going recalculate it. */
        size = size - sizeof(sw_state[_unit]->dpp.soc.arad.pp->vrrp_info);
        sw_state_access[_unit].dpp.soc.arad.pp.vrrp_info.get_size (  _unit, &temp_size);
        size = size + temp_size;
    }
    
    
    is_param_allocated = (sw_state[_unit]->dpp.soc.arad.pp->lif_table) ? 1 : 0;
    if (is_param_allocated) {
    /* need to reduce the size of sw_state[_unit]->dpp.soc.arad.pp->lif_table because  it's included in the 
        above and we are going recalculate it. */
        size = size - sizeof(sw_state[_unit]->dpp.soc.arad.pp->lif_table);
        sw_state_access[_unit].dpp.soc.arad.pp.lif_table.get_size (  _unit, &temp_size);
        size = size + temp_size;
    }
    
    
    is_param_allocated = (sw_state[_unit]->dpp.soc.arad.pp->header_data) ? 1 : 0;
    if (is_param_allocated) {
    /* need to reduce the size of sw_state[_unit]->dpp.soc.arad.pp->header_data because  it's included in the 
        above and we are going recalculate it. */
        size = size - sizeof(sw_state[_unit]->dpp.soc.arad.pp->header_data);
        sw_state_access[_unit].dpp.soc.arad.pp.header_data.get_size (  _unit, &temp_size);
        size = size + temp_size;
    }
    
    
    is_param_allocated = (sw_state[_unit]->dpp.soc.arad.pp->fwd_mact) ? 1 : 0;
    if (is_param_allocated) {
    /* need to reduce the size of sw_state[_unit]->dpp.soc.arad.pp->fwd_mact because  it's included in the 
        above and we are going recalculate it. */
        size = size - sizeof(sw_state[_unit]->dpp.soc.arad.pp->fwd_mact);
        sw_state_access[_unit].dpp.soc.arad.pp.fwd_mact.get_size (  _unit, &temp_size);
        size = size + temp_size;
    }
    
    
    is_param_allocated = (sw_state[_unit]->dpp.soc.arad.pp->lag) ? 1 : 0;
    if (is_param_allocated) {
    /* need to reduce the size of sw_state[_unit]->dpp.soc.arad.pp->lag because  it's included in the 
        above and we are going recalculate it. */
        size = size - sizeof(sw_state[_unit]->dpp.soc.arad.pp->lag);
        sw_state_access[_unit].dpp.soc.arad.pp.lag.get_size (  _unit, &temp_size);
        size = size + temp_size;
    }
    
    
    is_param_allocated = (sw_state[_unit]->dpp.soc.arad.pp->ilm_info) ? 1 : 0;
    if (is_param_allocated) {
    /* need to reduce the size of sw_state[_unit]->dpp.soc.arad.pp->ilm_info because  it's included in the 
        above and we are going recalculate it. */
        size = size - sizeof(sw_state[_unit]->dpp.soc.arad.pp->ilm_info);
        sw_state_access[_unit].dpp.soc.arad.pp.ilm_info.get_size (  _unit, &temp_size);
        size = size + temp_size;
    }
    
    
    is_param_allocated = (sw_state[_unit]->dpp.soc.arad.pp->oam_my_mac_lsb) ? 1 : 0;
    if (is_param_allocated) {
    /* need to reduce the size of sw_state[_unit]->dpp.soc.arad.pp->oam_my_mac_lsb because  it's included in the 
        above and we are going recalculate it. */
        size = size - sizeof(sw_state[_unit]->dpp.soc.arad.pp->oam_my_mac_lsb);
        sw_state_access[_unit].dpp.soc.arad.pp.oam_my_mac_lsb.get_size (  _unit, &temp_size);
        size = size + temp_size;
    }
    
    
    is_param_allocated = (sw_state[_unit]->dpp.soc.arad.pp->diag) ? 1 : 0;
    if (is_param_allocated) {
    /* need to reduce the size of sw_state[_unit]->dpp.soc.arad.pp->diag because  it's included in the 
        above and we are going recalculate it. */
        size = size - sizeof(sw_state[_unit]->dpp.soc.arad.pp->diag);
        sw_state_access[_unit].dpp.soc.arad.pp.diag.get_size (  _unit, &temp_size);
        size = size + temp_size;
    }
    
    
    is_param_allocated = (sw_state[_unit]->dpp.soc.arad.pp->lif_cos) ? 1 : 0;
    if (is_param_allocated) {
    /* need to reduce the size of sw_state[_unit]->dpp.soc.arad.pp->lif_cos because  it's included in the 
        above and we are going recalculate it. */
        size = size - sizeof(sw_state[_unit]->dpp.soc.arad.pp->lif_cos);
        sw_state_access[_unit].dpp.soc.arad.pp.lif_cos.get_size (  _unit, &temp_size);
        size = size + temp_size;
    }
    
    
    is_param_allocated = (sw_state[_unit]->dpp.soc.arad.pp->occupation) ? 1 : 0;
    if (is_param_allocated) {
    /* need to reduce the size of sw_state[_unit]->dpp.soc.arad.pp->occupation because  it's included in the 
        above and we are going recalculate it. */
        size = size - sizeof(sw_state[_unit]->dpp.soc.arad.pp->occupation);
        sw_state_access[_unit].dpp.soc.arad.pp.occupation.get_size (  _unit, &temp_size);
        size = size + temp_size;
    }
    
    
    is_param_allocated = (sw_state[_unit]->dpp.soc.arad.pp->fec) ? 1 : 0;
    if (is_param_allocated) {
    /* need to reduce the size of sw_state[_unit]->dpp.soc.arad.pp->fec because  it's included in the 
        above and we are going recalculate it. */
        size = size - sizeof(sw_state[_unit]->dpp.soc.arad.pp->fec);
        sw_state_access[_unit].dpp.soc.arad.pp.fec.get_size (  _unit, &temp_size);
        size = size + temp_size;
    }
    
    
    is_param_allocated = (sw_state[_unit]->dpp.soc.arad.pp->slb_config) ? 1 : 0;
    if (is_param_allocated) {
    /* need to reduce the size of sw_state[_unit]->dpp.soc.arad.pp->slb_config because  it's included in the 
        above and we are going recalculate it. */
        size = size - sizeof(sw_state[_unit]->dpp.soc.arad.pp->slb_config);
        sw_state_access[_unit].dpp.soc.arad.pp.slb_config.get_size (  _unit, &temp_size);
        size = size + temp_size;
    }
    
    
    is_param_allocated = (sw_state[_unit]->dpp.soc.arad.pp->arad_sw_db_src_binds) ? 1 : 0;
    if (is_param_allocated) {
    /* need to reduce the size of sw_state[_unit]->dpp.soc.arad.pp->arad_sw_db_src_binds because  it's included in the 
        above and we are going recalculate it. */
        size = size - sizeof(sw_state[_unit]->dpp.soc.arad.pp->arad_sw_db_src_binds);
        sw_state_access[_unit].dpp.soc.arad.pp.arad_sw_db_src_binds.get_size (  _unit, &temp_size);
        size = size + temp_size;
    }
    
    
    is_param_allocated = (sw_state[_unit]->dpp.soc.arad.pp->pp_port_map) ? 1 : 0;
    if (is_param_allocated) {
    /* need to reduce the size of sw_state[_unit]->dpp.soc.arad.pp->pp_port_map because  it's included in the 
        above and we are going recalculate it. */
        size = size - sizeof(sw_state[_unit]->dpp.soc.arad.pp->pp_port_map);
        sw_state_access[_unit].dpp.soc.arad.pp.pp_port_map.get_size (  _unit, &temp_size);
        size = size + temp_size;
    }
    
    
    is_param_allocated = (sw_state[_unit]->dpp.soc.arad.pp->extender_info) ? 1 : 0;
    if (is_param_allocated) {
    /* need to reduce the size of sw_state[_unit]->dpp.soc.arad.pp->extender_info because  it's included in the 
        above and we are going recalculate it. */
        size = size - sizeof(sw_state[_unit]->dpp.soc.arad.pp->extender_info);
        sw_state_access[_unit].dpp.soc.arad.pp.extender_info.get_size (  _unit, &temp_size);
        size = size + temp_size;
    }
    
    #if defined(BCM_88675_A0)&&defined(INCLUDE_KBP)&&!defined(BCM_88030)

    is_param_allocated = (sw_state[_unit]->dpp.soc.arad.pp->kaps_db) ? 1 : 0;
    if (is_param_allocated) {
    /* need to reduce the size of sw_state[_unit]->dpp.soc.arad.pp->kaps_db because  it's included in the 
        above and we are going recalculate it. */
        size = size - sizeof(sw_state[_unit]->dpp.soc.arad.pp->kaps_db);
        sw_state_access[_unit].dpp.soc.arad.pp.kaps_db.get_size (  _unit, &temp_size);
        size = size + temp_size;
    }
    #endif /* defined(BCM_88675_A0)&&defined(INCLUDE_KBP)&&!defined(BCM_88030)*/ 

    
    is_param_allocated = (sw_state[_unit]->dpp.soc.arad.pp->vtt) ? 1 : 0;
    if (is_param_allocated) {
    /* need to reduce the size of sw_state[_unit]->dpp.soc.arad.pp->vtt because  it's included in the 
        above and we are going recalculate it. */
        size = size - sizeof(sw_state[_unit]->dpp.soc.arad.pp->vtt);
        sw_state_access[_unit].dpp.soc.arad.pp.vtt.get_size (  _unit, &temp_size);
        size = size + temp_size;
    }
    
    
    is_param_allocated = (sw_state[_unit]->dpp.soc.arad.pp->frwrd_ip) ? 1 : 0;
    if (is_param_allocated) {
    /* need to reduce the size of sw_state[_unit]->dpp.soc.arad.pp->frwrd_ip because  it's included in the 
        above and we are going recalculate it. */
        size = size - sizeof(sw_state[_unit]->dpp.soc.arad.pp->frwrd_ip);
        sw_state_access[_unit].dpp.soc.arad.pp.frwrd_ip.get_size (  _unit, &temp_size);
        size = size + temp_size;
    }
    
    
    is_param_allocated = (sw_state[_unit]->dpp.soc.arad.pp->l2_lif) ? 1 : 0;
    if (is_param_allocated) {
    /* need to reduce the size of sw_state[_unit]->dpp.soc.arad.pp->l2_lif because  it's included in the 
        above and we are going recalculate it. */
        size = size - sizeof(sw_state[_unit]->dpp.soc.arad.pp->l2_lif);
        sw_state_access[_unit].dpp.soc.arad.pp.l2_lif.get_size (  _unit, &temp_size);
        size = size + temp_size;
    }
    
    
    is_param_allocated = (sw_state[_unit]->dpp.soc.arad.pp->l2_lif_ac) ? 1 : 0;
    if (is_param_allocated) {
    /* need to reduce the size of sw_state[_unit]->dpp.soc.arad.pp->l2_lif_ac because  it's included in the 
        above and we are going recalculate it. */
        size = size - sizeof(sw_state[_unit]->dpp.soc.arad.pp->l2_lif_ac);
        sw_state_access[_unit].dpp.soc.arad.pp.l2_lif_ac.get_size (  _unit, &temp_size);
        size = size + temp_size;
    }
    
    
    /* need to reduce the size of sw_state[_unit]->dpp.soc.arad.pp->eth_policer_mtr_profile because  it's included in the 
        above and we are going recalculate it. */
    size = size - sizeof(sw_state[_unit]->dpp.soc.arad.pp->eth_policer_mtr_profile);
    for (idx0 = 0 ; idx0 <  2 ; idx0++) { 
            sw_state_access[_unit].dpp.soc.arad.pp.eth_policer_mtr_profile.get_size (  _unit, idx0, &temp_size);
            size = size + temp_size;
    }
    
    
    is_param_allocated = (sw_state[_unit]->dpp.soc.arad.pp->llp_cos) ? 1 : 0;
    if (is_param_allocated) {
    /* need to reduce the size of sw_state[_unit]->dpp.soc.arad.pp->llp_cos because  it's included in the 
        above and we are going recalculate it. */
        size = size - sizeof(sw_state[_unit]->dpp.soc.arad.pp->llp_cos);
        sw_state_access[_unit].dpp.soc.arad.pp.llp_cos.get_size (  _unit, &temp_size);
        size = size + temp_size;
    }
    
    
    is_param_allocated = (sw_state[_unit]->dpp.soc.arad.pp->eg_encap) ? 1 : 0;
    if (is_param_allocated) {
    /* need to reduce the size of sw_state[_unit]->dpp.soc.arad.pp->eg_encap because  it's included in the 
        above and we are going recalculate it. */
        size = size - sizeof(sw_state[_unit]->dpp.soc.arad.pp->eg_encap);
        sw_state_access[_unit].dpp.soc.arad.pp.eg_encap.get_size (  _unit, &temp_size);
        size = size + temp_size;
    }
    
    
    is_param_allocated = (sw_state[_unit]->dpp.soc.arad.pp->llp_mirror) ? 1 : 0;
    if (is_param_allocated) {
    /* need to reduce the size of sw_state[_unit]->dpp.soc.arad.pp->llp_mirror because  it's included in the 
        above and we are going recalculate it. */
        size = size - sizeof(sw_state[_unit]->dpp.soc.arad.pp->llp_mirror);
        sw_state_access[_unit].dpp.soc.arad.pp.llp_mirror.get_size (  _unit, &temp_size);
        size = size + temp_size;
    }
    
    
    is_param_allocated = (sw_state[_unit]->dpp.soc.arad.pp->eg_mirror) ? 1 : 0;
    if (is_param_allocated) {
    /* need to reduce the size of sw_state[_unit]->dpp.soc.arad.pp->eg_mirror because  it's included in the 
        above and we are going recalculate it. */
        size = size - sizeof(sw_state[_unit]->dpp.soc.arad.pp->eg_mirror);
        sw_state_access[_unit].dpp.soc.arad.pp.eg_mirror.get_size (  _unit, &temp_size);
        size = size + temp_size;
    }
    
    
    is_param_allocated = (sw_state[_unit]->dpp.soc.arad.pp->llp_trap) ? 1 : 0;
    if (is_param_allocated) {
    /* need to reduce the size of sw_state[_unit]->dpp.soc.arad.pp->llp_trap because  it's included in the 
        above and we are going recalculate it. */
        size = size - sizeof(sw_state[_unit]->dpp.soc.arad.pp->llp_trap);
        sw_state_access[_unit].dpp.soc.arad.pp.llp_trap.get_size (  _unit, &temp_size);
        size = size + temp_size;
    }
    
    
    is_param_allocated = (sw_state[_unit]->dpp.soc.arad.pp->ipv4_pat) ? 1 : 0;
    if (is_param_allocated) {
    /* need to reduce the size of sw_state[_unit]->dpp.soc.arad.pp->ipv4_pat because  it's included in the 
        above and we are going recalculate it. */
        size = size - sizeof(sw_state[_unit]->dpp.soc.arad.pp->ipv4_pat);
        sw_state_access[_unit].dpp.soc.arad.pp.ipv4_pat.get_size (  _unit, &temp_size);
        size = size + temp_size;
    }
    
    
    is_param_allocated = (sw_state[_unit]->dpp.soc.arad.pp->oam) ? 1 : 0;
    if (is_param_allocated) {
    /* need to reduce the size of sw_state[_unit]->dpp.soc.arad.pp->oam because  it's included in the 
        above and we are going recalculate it. */
        size = size - sizeof(sw_state[_unit]->dpp.soc.arad.pp->oam);
        sw_state_access[_unit].dpp.soc.arad.pp.oam.get_size (  _unit, &temp_size);
        size = size + temp_size;
    }
    
    
    is_param_allocated = (sw_state[_unit]->dpp.soc.arad.pp->bfd) ? 1 : 0;
    if (is_param_allocated) {
    /* need to reduce the size of sw_state[_unit]->dpp.soc.arad.pp->bfd because  it's included in the 
        above and we are going recalculate it. */
        size = size - sizeof(sw_state[_unit]->dpp.soc.arad.pp->bfd);
        sw_state_access[_unit].dpp.soc.arad.pp.bfd.get_size (  _unit, &temp_size);
        size = size + temp_size;
    }
    
    
    is_param_allocated = (sw_state[_unit]->dpp.soc.arad.pp->oper_mode) ? 1 : 0;
    if (is_param_allocated) {
    /* need to reduce the size of sw_state[_unit]->dpp.soc.arad.pp->oper_mode because  it's included in the 
        above and we are going recalculate it. */
        size = size - sizeof(sw_state[_unit]->dpp.soc.arad.pp->oper_mode);
        sw_state_access[_unit].dpp.soc.arad.pp.oper_mode.get_size (  _unit, &temp_size);
        size = size + temp_size;
    }
    
    
    is_param_allocated = (sw_state[_unit]->dpp.soc.arad.pp->overlay_arp) ? 1 : 0;
    if (is_param_allocated) {
    /* need to reduce the size of sw_state[_unit]->dpp.soc.arad.pp->overlay_arp because  it's included in the 
        above and we are going recalculate it. */
        size = size - sizeof(sw_state[_unit]->dpp.soc.arad.pp->overlay_arp);
        sw_state_access[_unit].dpp.soc.arad.pp.overlay_arp.get_size (  _unit, &temp_size);
        size = size + temp_size;
    }
    
    
    is_param_allocated = (sw_state[_unit]->dpp.soc.arad.pp->ipv4_info) ? 1 : 0;
    if (is_param_allocated) {
    /* need to reduce the size of sw_state[_unit]->dpp.soc.arad.pp->ipv4_info because  it's included in the 
        above and we are going recalculate it. */
        size = size - sizeof(sw_state[_unit]->dpp.soc.arad.pp->ipv4_info);
        sw_state_access[_unit].dpp.soc.arad.pp.ipv4_info.get_size (  _unit, &temp_size);
        size = size + temp_size;
    }
    
    
    is_param_allocated = (sw_state[_unit]->dpp.soc.arad.pp->rif_to_lif_group_map) ? 1 : 0;
    if (is_param_allocated) {
    /* need to reduce the size of sw_state[_unit]->dpp.soc.arad.pp->rif_to_lif_group_map because  it's included in the 
        above and we are going recalculate it. */
        size = size - sizeof(sw_state[_unit]->dpp.soc.arad.pp->rif_to_lif_group_map);
        sw_state_access[_unit].dpp.soc.arad.pp.rif_to_lif_group_map.get_size (  _unit, &temp_size);
        size = size + temp_size;
    }
    
    
    is_param_allocated = (sw_state[_unit]->dpp.soc.arad.pp->vdc_vsi_map) ? 1 : 0;
    if (is_param_allocated) {
        shr_sw_state_allocated_size_get (_unit, (uint8*)sw_state[_unit]->dpp.soc.arad.pp->vdc_vsi_map, &allocated_size);
        toIdx0 = allocated_size /
             sizeof(*(sw_state[_unit]->dpp.soc.arad.pp->vdc_vsi_map));
        for (idx0 = 0; idx0 < toIdx0; idx0++) {
            sw_state_access[_unit].dpp.soc.arad.pp.vdc_vsi_map.get_size (  _unit, idx0, &temp_size);
            size = size + temp_size;
        }
    }
    
    *ret_size = size;
    return rv;
}



int
sw_state_dpp_soc_arad_access_cb_init(int _unit){
    sw_state_dpp_soc_arad_pp_arad_sw_db_src_binds_access_cb_init(_unit);
    sw_state_dpp_soc_arad_pp_bfd_access_cb_init(_unit);
    sw_state_dpp_soc_arad_pp_dbal_info_access_cb_init(_unit);
    sw_state_dpp_soc_arad_pp_diag_access_cb_init(_unit);
    sw_state_dpp_soc_arad_pp_ecmp_info_access_cb_init(_unit);
    sw_state_dpp_soc_arad_pp_eg_encap_access_cb_init(_unit);
    sw_state_dpp_soc_arad_pp_eg_mirror_access_cb_init(_unit);
    sw_state_dpp_soc_arad_pp_eth_policer_mtr_profile_access_cb_init(_unit);
    sw_state_dpp_soc_arad_pp_extender_info_access_cb_init(_unit);
    sw_state_dpp_soc_arad_pp_fec_access_cb_init(_unit);
    sw_state_dpp_soc_arad_pp_frwrd_ip_access_cb_init(_unit);
    sw_state_dpp_soc_arad_pp_fwd_mact_access_cb_init(_unit);
    sw_state_dpp_soc_arad_pp_header_data_access_cb_init(_unit);
    sw_state_dpp_soc_arad_pp_ilm_info_access_cb_init(_unit);
    sw_state_dpp_soc_arad_pp_ipv4_info_access_cb_init(_unit);
    sw_state_dpp_soc_arad_pp_ipv4_pat_access_cb_init(_unit);
    sw_state_dpp_soc_arad_pp_kaps_db_access_cb_init(_unit);
    sw_state_dpp_soc_arad_pp_l2_lif_access_cb_init(_unit);
    sw_state_dpp_soc_arad_pp_l2_lif_ac_access_cb_init(_unit);
    sw_state_dpp_soc_arad_pp_lag_access_cb_init(_unit);
    sw_state_dpp_soc_arad_pp_lif_cos_access_cb_init(_unit);
    sw_state_dpp_soc_arad_pp_lif_table_access_cb_init(_unit);
    sw_state_dpp_soc_arad_pp_llp_cos_access_cb_init(_unit);
    sw_state_dpp_soc_arad_pp_llp_filter_access_cb_init(_unit);
    sw_state_dpp_soc_arad_pp_llp_mirror_access_cb_init(_unit);
    sw_state_dpp_soc_arad_pp_llp_trap_access_cb_init(_unit);
    sw_state_dpp_soc_arad_pp_llp_vid_assign_access_cb_init(_unit);
    sw_state_dpp_soc_arad_pp_oam_access_cb_init(_unit);
    sw_state_dpp_soc_arad_pp_oam_my_mac_lsb_access_cb_init(_unit);
    sw_state_dpp_soc_arad_pp_occupation_access_cb_init(_unit);
    sw_state_dpp_soc_arad_pp_oper_mode_access_cb_init(_unit);
    sw_state_dpp_soc_arad_pp_overlay_arp_access_cb_init(_unit);
    sw_state_dpp_soc_arad_pp_pon_double_lookup_access_cb_init(_unit);
    sw_state_dpp_soc_arad_pp_pp_port_map_access_cb_init(_unit);
    sw_state_dpp_soc_arad_pp_rif_to_lif_group_map_access_cb_init(_unit);
    sw_state_dpp_soc_arad_pp_slb_config_access_cb_init(_unit);
    sw_state_dpp_soc_arad_pp_vdc_vsi_map_access_cb_init(_unit);
    sw_state_dpp_soc_arad_pp_vrrp_info_access_cb_init(_unit);
    sw_state_dpp_soc_arad_pp_vtt_access_cb_init(_unit);
    sw_state_dpp_soc_arad_tm_arad_egr_ports_access_cb_init(_unit);
    sw_state_dpp_soc_arad_tm_arad_em_blocks_access_cb_init(_unit);
    sw_state_dpp_soc_arad_tm_arad_multicast_access_cb_init(_unit);
    sw_state_dpp_soc_arad_tm_arad_plus_access_cb_init(_unit);
    sw_state_dpp_soc_arad_tm_cell_access_cb_init(_unit);
    sw_state_dpp_soc_arad_tm_chip_definitions_access_cb_init(_unit);
    sw_state_dpp_soc_arad_tm_dram_access_cb_init(_unit);
    sw_state_dpp_soc_arad_tm_egr_prog_editor_access_cb_init(_unit);
    sw_state_dpp_soc_arad_tm_guaranteed_q_resource_access_cb_init(_unit);
    sw_state_dpp_soc_arad_tm_interrupts_access_cb_init(_unit);
    sw_state_dpp_soc_arad_tm_jer_modid_group_map_access_cb_init(_unit);
    sw_state_dpp_soc_arad_tm_kbp_info_access_cb_init(_unit);
    sw_state_dpp_soc_arad_tm_lag_access_cb_init(_unit);
    sw_state_dpp_soc_arad_tm_logical_ports_info_access_cb_init(_unit);
    sw_state_dpp_soc_arad_tm_modport2sysport_access_cb_init(_unit);
    sw_state_dpp_soc_arad_tm_op_mode_access_cb_init(_unit);
    sw_state_dpp_soc_arad_tm_phy_ports_info_access_cb_init(_unit);
    sw_state_dpp_soc_arad_tm_pmf_access_cb_init(_unit);
    sw_state_dpp_soc_arad_tm_q_type_map_access_cb_init(_unit);
    sw_state_dpp_soc_arad_tm_q_type_ref_count_access_cb_init(_unit);
    sw_state_dpp_soc_arad_tm_reassembly_ctxt_access_cb_init(_unit);
    sw_state_dpp_soc_arad_tm_tcam_access_cb_init(_unit);
    sw_state_dpp_soc_arad_tm_tcam_mgmt_access_cb_init(_unit);
    sw_state_dpp_soc_arad_tm_tdm_access_cb_init(_unit);
    sw_state_dpp_soc_arad_tm_tm_info_access_cb_init(_unit);
    sw_state_dpp_soc_arad_tm_vsi_access_cb_init(_unit);
    sw_state_access[_unit].dpp.soc.arad.verify = sw_state_dpp_soc_arad_verify;
    sw_state_access[_unit].dpp.soc.arad.get_size = sw_state_dpp_soc_arad_get_size;
    sw_state_access[_unit].dpp.soc.arad.tm.alloc = sw_state_dpp_soc_arad_tm_alloc;
    sw_state_access[_unit].dpp.soc.arad.tm.is_allocated = sw_state_dpp_soc_arad_tm_is_allocated;
    sw_state_access[_unit].dpp.soc.arad.tm.free = sw_state_dpp_soc_arad_tm_free;
    sw_state_access[_unit].dpp.soc.arad.tm.verify = sw_state_dpp_soc_arad_tm_verify;
    sw_state_access[_unit].dpp.soc.arad.tm.get_size = sw_state_dpp_soc_arad_tm_get_size;
    sw_state_access[_unit].dpp.soc.arad.pp.alloc = sw_state_dpp_soc_arad_pp_alloc;
    sw_state_access[_unit].dpp.soc.arad.pp.is_allocated = sw_state_dpp_soc_arad_pp_is_allocated;
    sw_state_access[_unit].dpp.soc.arad.pp.free = sw_state_dpp_soc_arad_pp_free;
    sw_state_access[_unit].dpp.soc.arad.pp.verify = sw_state_dpp_soc_arad_pp_verify;
    sw_state_access[_unit].dpp.soc.arad.pp.get_size = sw_state_dpp_soc_arad_pp_get_size;
    return _SHR_E_NONE;
}
