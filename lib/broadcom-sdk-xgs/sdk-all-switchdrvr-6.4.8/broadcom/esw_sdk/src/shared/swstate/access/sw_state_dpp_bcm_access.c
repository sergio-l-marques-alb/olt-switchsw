/* $Id: $
 * 
 * $Copyright: Copyright 2015 Broadcom Corporation.
 * This program is the proprietary software of Broadcom Corporation
 * and/or its licensors, and may only be used, duplicated, modified
 * or distributed pursuant to the terms and conditions of a separate,
 * written license agreement executed between you and Broadcom
 * (an "Authorized License").  Except as set forth in an Authorized
 * License, Broadcom grants no license (express or implied), right
 * to use, or waiver of any kind with respect to the Software, and
 * Broadcom expressly reserves all rights in and to the Software
 * and all intellectual property rights therein.  IF YOU HAVE
 * NO AUTHORIZED LICENSE, THEN YOU HAVE NO RIGHT TO USE THIS SOFTWARE
 * IN ANY WAY, AND SHOULD IMMEDIATELY NOTIFY BROADCOM AND DISCONTINUE
 * ALL USE OF THE SOFTWARE.  
 *  
 * Except as expressly set forth in the Authorized License,
 *  
 * 1.     This program, including its structure, sequence and organization,
 * constitutes the valuable trade secrets of Broadcom, and you shall use
 * all reasonable efforts to protect the confidentiality thereof,
 * and to use this information only in connection with your use of
 * Broadcom integrated circuit products.
 *  
 * 2.     TO THE MAXIMUM EXTENT PERMITTED BY LAW, THE SOFTWARE IS
 * PROVIDED "AS IS" AND WITH ALL FAULTS AND BROADCOM MAKES NO PROMISES,
 * REPRESENTATIONS OR WARRANTIES, EITHER EXPRESS, IMPLIED, STATUTORY,
 * OR OTHERWISE, WITH RESPECT TO THE SOFTWARE.  BROADCOM SPECIFICALLY
 * DISCLAIMS ANY AND ALL IMPLIED WARRANTIES OF TITLE, MERCHANTABILITY,
 * NONINFRINGEMENT, FITNESS FOR A PARTICULAR PURPOSE, LACK OF VIRUSES,
 * ACCURACY OR COMPLETENESS, QUIET ENJOYMENT, QUIET POSSESSION OR
 * CORRESPONDENCE TO DESCRIPTION. YOU ASSUME THE ENTIRE RISK ARISING
 * OUT OF USE OR PERFORMANCE OF THE SOFTWARE.
 * 
 * 3.     TO THE MAXIMUM EXTENT PERMITTED BY LAW, IN NO EVENT SHALL
 * BROADCOM OR ITS LICENSORS BE LIABLE FOR (i) CONSEQUENTIAL,
 * INCIDENTAL, SPECIAL, INDIRECT, OR EXEMPLARY DAMAGES WHATSOEVER
 * ARISING OUT OF OR IN ANY WAY RELATING TO YOUR USE OF OR INABILITY
 * TO USE THE SOFTWARE EVEN IF BROADCOM HAS BEEN ADVISED OF THE
 * POSSIBILITY OF SUCH DAMAGES; OR (ii) ANY AMOUNT IN EXCESS OF
 * THE AMOUNT ACTUALLY PAID FOR THE SOFTWARE ITSELF OR USD 1.00,
 * WHICHEVER IS GREATER. THESE LIMITATIONS SHALL APPLY NOTWITHSTANDING
 * ANY FAILURE OF ESSENTIAL PURPOSE OF ANY LIMITED REMEDY.$
 * 
 * DO NOT EDIT THIS FILE!
 * This file is auto-generated.
 * Edits to this file will be lost when it is regenerated.
 */

#include <sal/core/libc.h>
#include <shared/swstate/sw_state.h>
#include <shared/error.h>
#include <shared/swstate/sw_state_defs.h>
#include <shared/bsl.h>
#include <shared/swstate/access/sw_state_access.h>
#include <shared/swstate/access/sw_state_dpp_bcm_access.h>


/* AUTO-GENERATED - DO NOT MODIFY */
int sw_state_dpp_bcm_verify(int _unit){
    int rv = _SHR_E_NONE;
    rv = sw_state_access[_unit].dpp.verify( _unit);
    if (rv != _SHR_E_NONE) return rv;
    return rv;
}


/* AUTO-GENERATED - DO NOT MODIFY */
int sw_state_dpp_bcm_get_size(int _unit, int *ret_size){
    int rv = _SHR_E_NONE;
    int size = 0;
    INIT_INDEXES();
    size = size + sizeof(sw_state[_unit]->dpp.bcm);
    
    is_param_allocated = (sw_state[_unit]->dpp.bcm.stg) ? 1 : 0;
    if (is_param_allocated) {
    /* need to reduce the size of sw_state[_unit]->dpp.bcm.stg because  it's included in the 
        above and we are going recalculate it. */
        size = size - sizeof(sw_state[_unit]->dpp.bcm.stg);
        sw_state_access[_unit].dpp.bcm.stg.get_size (  _unit, &temp_size);
        size = size + temp_size;
    }
    
    
    is_param_allocated = (sw_state[_unit]->dpp.bcm.policer) ? 1 : 0;
    if (is_param_allocated) {
    /* need to reduce the size of sw_state[_unit]->dpp.bcm.policer because  it's included in the 
        above and we are going recalculate it. */
        size = size - sizeof(sw_state[_unit]->dpp.bcm.policer);
        sw_state_access[_unit].dpp.bcm.policer.get_size (  _unit, &temp_size);
        size = size + temp_size;
    }
    
    
    is_param_allocated = (sw_state[_unit]->dpp.bcm.counter) ? 1 : 0;
    if (is_param_allocated) {
    /* need to reduce the size of sw_state[_unit]->dpp.bcm.counter because  it's included in the 
        above and we are going recalculate it. */
        size = size - sizeof(sw_state[_unit]->dpp.bcm.counter);
        sw_state_access[_unit].dpp.bcm.counter.get_size (  _unit, &temp_size);
        size = size + temp_size;
    }
    
    
    is_param_allocated = (sw_state[_unit]->dpp.bcm.mirror) ? 1 : 0;
    if (is_param_allocated) {
    /* need to reduce the size of sw_state[_unit]->dpp.bcm.mirror because  it's included in the 
        above and we are going recalculate it. */
        size = size - sizeof(sw_state[_unit]->dpp.bcm.mirror);
        sw_state_access[_unit].dpp.bcm.mirror.get_size (  _unit, &temp_size);
        size = size + temp_size;
    }
    
    
    is_param_allocated = (sw_state[_unit]->dpp.bcm.l3) ? 1 : 0;
    if (is_param_allocated) {
    /* need to reduce the size of sw_state[_unit]->dpp.bcm.l3 because  it's included in the 
        above and we are going recalculate it. */
        size = size - sizeof(sw_state[_unit]->dpp.bcm.l3);
        sw_state_access[_unit].dpp.bcm.l3.get_size (  _unit, &temp_size);
        size = size + temp_size;
    }
    
    
    is_param_allocated = (sw_state[_unit]->dpp.bcm.l2) ? 1 : 0;
    if (is_param_allocated) {
    /* need to reduce the size of sw_state[_unit]->dpp.bcm.l2 because  it's included in the 
        above and we are going recalculate it. */
        size = size - sizeof(sw_state[_unit]->dpp.bcm.l2);
        sw_state_access[_unit].dpp.bcm.l2.get_size (  _unit, &temp_size);
        size = size + temp_size;
    }
    
    
    is_param_allocated = (sw_state[_unit]->dpp.bcm.trunk) ? 1 : 0;
    if (is_param_allocated) {
    /* need to reduce the size of sw_state[_unit]->dpp.bcm.trunk because  it's included in the 
        above and we are going recalculate it. */
        size = size - sizeof(sw_state[_unit]->dpp.bcm.trunk);
        sw_state_access[_unit].dpp.bcm.trunk.get_size (  _unit, &temp_size);
        size = size + temp_size;
    }
    
    
    is_param_allocated = (sw_state[_unit]->dpp.bcm.vlan) ? 1 : 0;
    if (is_param_allocated) {
    /* need to reduce the size of sw_state[_unit]->dpp.bcm.vlan because  it's included in the 
        above and we are going recalculate it. */
        size = size - sizeof(sw_state[_unit]->dpp.bcm.vlan);
        sw_state_access[_unit].dpp.bcm.vlan.get_size (  _unit, &temp_size);
        size = size + temp_size;
    }
    
    
    is_param_allocated = (sw_state[_unit]->dpp.bcm.vswitch) ? 1 : 0;
    if (is_param_allocated) {
    /* need to reduce the size of sw_state[_unit]->dpp.bcm.vswitch because  it's included in the 
        above and we are going recalculate it. */
        size = size - sizeof(sw_state[_unit]->dpp.bcm.vswitch);
        sw_state_access[_unit].dpp.bcm.vswitch.get_size (  _unit, &temp_size);
        size = size + temp_size;
    }
    
    
    is_param_allocated = (sw_state[_unit]->dpp.bcm.rx) ? 1 : 0;
    if (is_param_allocated) {
    /* need to reduce the size of sw_state[_unit]->dpp.bcm.rx because  it's included in the 
        above and we are going recalculate it. */
        size = size - sizeof(sw_state[_unit]->dpp.bcm.rx);
        sw_state_access[_unit].dpp.bcm.rx.get_size (  _unit, &temp_size);
        size = size + temp_size;
    }
    
    
    is_param_allocated = (sw_state[_unit]->dpp.bcm.failover) ? 1 : 0;
    if (is_param_allocated) {
    /* need to reduce the size of sw_state[_unit]->dpp.bcm.failover because  it's included in the 
        above and we are going recalculate it. */
        size = size - sizeof(sw_state[_unit]->dpp.bcm.failover);
        sw_state_access[_unit].dpp.bcm.failover.get_size (  _unit, &temp_size);
        size = size + temp_size;
    }
    
    
    is_param_allocated = (sw_state[_unit]->dpp.bcm.gport_mgmt) ? 1 : 0;
    if (is_param_allocated) {
    /* need to reduce the size of sw_state[_unit]->dpp.bcm.gport_mgmt because  it's included in the 
        above and we are going recalculate it. */
        size = size - sizeof(sw_state[_unit]->dpp.bcm.gport_mgmt);
        sw_state_access[_unit].dpp.bcm.gport_mgmt.get_size (  _unit, &temp_size);
        size = size + temp_size;
    }
    
    
    is_param_allocated = (sw_state[_unit]->dpp.bcm.bfd) ? 1 : 0;
    if (is_param_allocated) {
    /* need to reduce the size of sw_state[_unit]->dpp.bcm.bfd because  it's included in the 
        above and we are going recalculate it. */
        size = size - sizeof(sw_state[_unit]->dpp.bcm.bfd);
        sw_state_access[_unit].dpp.bcm.bfd.get_size (  _unit, &temp_size);
        size = size + temp_size;
    }
    
    
    is_param_allocated = (sw_state[_unit]->dpp.bcm.alloc_mngr) ? 1 : 0;
    if (is_param_allocated) {
    /* need to reduce the size of sw_state[_unit]->dpp.bcm.alloc_mngr because  it's included in the 
        above and we are going recalculate it. */
        size = size - sizeof(sw_state[_unit]->dpp.bcm.alloc_mngr);
        sw_state_access[_unit].dpp.bcm.alloc_mngr.get_size (  _unit, &temp_size);
        size = size + temp_size;
    }
    
    
    is_param_allocated = (sw_state[_unit]->dpp.bcm.alloc_mngr_utils) ? 1 : 0;
    if (is_param_allocated) {
    /* need to reduce the size of sw_state[_unit]->dpp.bcm.alloc_mngr_utils because  it's included in the 
        above and we are going recalculate it. */
        size = size - sizeof(sw_state[_unit]->dpp.bcm.alloc_mngr_utils);
        sw_state_access[_unit].dpp.bcm.alloc_mngr_utils.get_size (  _unit, &temp_size);
        size = size + temp_size;
    }
    
    
    is_param_allocated = (sw_state[_unit]->dpp.bcm.alloc_mngr_glif) ? 1 : 0;
    if (is_param_allocated) {
    /* need to reduce the size of sw_state[_unit]->dpp.bcm.alloc_mngr_glif because  it's included in the 
        above and we are going recalculate it. */
        size = size - sizeof(sw_state[_unit]->dpp.bcm.alloc_mngr_glif);
        sw_state_access[_unit].dpp.bcm.alloc_mngr_glif.get_size (  _unit, &temp_size);
        size = size + temp_size;
    }
    
    
    is_param_allocated = (sw_state[_unit]->dpp.bcm.alloc_mngr_local_lif) ? 1 : 0;
    if (is_param_allocated) {
    /* need to reduce the size of sw_state[_unit]->dpp.bcm.alloc_mngr_local_lif because  it's included in the 
        above and we are going recalculate it. */
        size = size - sizeof(sw_state[_unit]->dpp.bcm.alloc_mngr_local_lif);
        sw_state_access[_unit].dpp.bcm.alloc_mngr_local_lif.get_size (  _unit, &temp_size);
        size = size + temp_size;
    }
    
    
    is_param_allocated = (sw_state[_unit]->dpp.bcm.ipmc) ? 1 : 0;
    if (is_param_allocated) {
    /* need to reduce the size of sw_state[_unit]->dpp.bcm.ipmc because  it's included in the 
        above and we are going recalculate it. */
        size = size - sizeof(sw_state[_unit]->dpp.bcm.ipmc);
        sw_state_access[_unit].dpp.bcm.ipmc.get_size (  _unit, &temp_size);
        size = size + temp_size;
    }
    
    
    is_param_allocated = (sw_state[_unit]->dpp.bcm._switch) ? 1 : 0;
    if (is_param_allocated) {
    /* need to reduce the size of sw_state[_unit]->dpp.bcm._switch because  it's included in the 
        above and we are going recalculate it. */
        size = size - sizeof(sw_state[_unit]->dpp.bcm._switch);
        sw_state_access[_unit].dpp.bcm._switch.get_size (  _unit, &temp_size);
        size = size + temp_size;
    }
    
    
    is_param_allocated = (sw_state[_unit]->dpp.bcm.port) ? 1 : 0;
    if (is_param_allocated) {
    /* need to reduce the size of sw_state[_unit]->dpp.bcm.port because  it's included in the 
        above and we are going recalculate it. */
        size = size - sizeof(sw_state[_unit]->dpp.bcm.port);
        sw_state_access[_unit].dpp.bcm.port.get_size (  _unit, &temp_size);
        size = size + temp_size;
    }
    
    
    is_param_allocated = (sw_state[_unit]->dpp.bcm.trill) ? 1 : 0;
    if (is_param_allocated) {
    /* need to reduce the size of sw_state[_unit]->dpp.bcm.trill because  it's included in the 
        above and we are going recalculate it. */
        size = size - sizeof(sw_state[_unit]->dpp.bcm.trill);
        sw_state_access[_unit].dpp.bcm.trill.get_size (  _unit, &temp_size);
        size = size + temp_size;
    }
    
    
    is_param_allocated = (sw_state[_unit]->dpp.bcm.stack) ? 1 : 0;
    if (is_param_allocated) {
    /* need to reduce the size of sw_state[_unit]->dpp.bcm.stack because  it's included in the 
        above and we are going recalculate it. */
        size = size - sizeof(sw_state[_unit]->dpp.bcm.stack);
        sw_state_access[_unit].dpp.bcm.stack.get_size (  _unit, &temp_size);
        size = size + temp_size;
    }
    
    
    is_param_allocated = (sw_state[_unit]->dpp.bcm.qos) ? 1 : 0;
    if (is_param_allocated) {
    /* need to reduce the size of sw_state[_unit]->dpp.bcm.qos because  it's included in the 
        above and we are going recalculate it. */
        size = size - sizeof(sw_state[_unit]->dpp.bcm.qos);
        sw_state_access[_unit].dpp.bcm.qos.get_size (  _unit, &temp_size);
        size = size + temp_size;
    }
    
    
    /* need to reduce the size of sw_state[_unit]->dpp.bcm.mim because  it's included in the 
        above and we are going recalculate it. */
    size = size - sizeof(sw_state[_unit]->dpp.bcm.mim);
    sw_state_access[_unit].dpp.bcm.mim.get_size (  _unit, &temp_size);
    size = size + temp_size;
    
    
    is_param_allocated = (sw_state[_unit]->dpp.bcm.oam) ? 1 : 0;
    if (is_param_allocated) {
    /* need to reduce the size of sw_state[_unit]->dpp.bcm.oam because  it's included in the 
        above and we are going recalculate it. */
        size = size - sizeof(sw_state[_unit]->dpp.bcm.oam);
        sw_state_access[_unit].dpp.bcm.oam.get_size (  _unit, &temp_size);
        size = size + temp_size;
    }
    
    
    is_param_allocated = (sw_state[_unit]->dpp.bcm.cosq) ? 1 : 0;
    if (is_param_allocated) {
    /* need to reduce the size of sw_state[_unit]->dpp.bcm.cosq because  it's included in the 
        above and we are going recalculate it. */
        size = size - sizeof(sw_state[_unit]->dpp.bcm.cosq);
        sw_state_access[_unit].dpp.bcm.cosq.get_size (  _unit, &temp_size);
        size = size + temp_size;
    }
    
    
    is_param_allocated = (sw_state[_unit]->dpp.bcm.field) ? 1 : 0;
    if (is_param_allocated) {
    /* need to reduce the size of sw_state[_unit]->dpp.bcm.field because  it's included in the 
        above and we are going recalculate it. */
        size = size - sizeof(sw_state[_unit]->dpp.bcm.field);
        sw_state_access[_unit].dpp.bcm.field.get_size (  _unit, &temp_size);
        size = size + temp_size;
    }
    
    *ret_size = size;
    return rv;
}



int
sw_state_dpp_bcm_access_cb_init(int _unit){
    sw_state_dpp_bcm__switch_access_cb_init(_unit);
    sw_state_dpp_bcm_alloc_mngr_access_cb_init(_unit);
    sw_state_dpp_bcm_alloc_mngr_glif_access_cb_init(_unit);
    sw_state_dpp_bcm_alloc_mngr_local_lif_access_cb_init(_unit);
    sw_state_dpp_bcm_alloc_mngr_utils_access_cb_init(_unit);
    sw_state_dpp_bcm_bfd_access_cb_init(_unit);
    sw_state_dpp_bcm_cosq_access_cb_init(_unit);
    sw_state_dpp_bcm_counter_access_cb_init(_unit);
    sw_state_dpp_bcm_failover_access_cb_init(_unit);
    sw_state_dpp_bcm_field_access_cb_init(_unit);
    sw_state_dpp_bcm_gport_mgmt_access_cb_init(_unit);
    sw_state_dpp_bcm_ipmc_access_cb_init(_unit);
    sw_state_dpp_bcm_l2_access_cb_init(_unit);
    sw_state_dpp_bcm_l3_access_cb_init(_unit);
    sw_state_dpp_bcm_mim_access_cb_init(_unit);
    sw_state_dpp_bcm_mirror_access_cb_init(_unit);
    sw_state_dpp_bcm_oam_access_cb_init(_unit);
    sw_state_dpp_bcm_policer_access_cb_init(_unit);
    sw_state_dpp_bcm_port_access_cb_init(_unit);
    sw_state_dpp_bcm_qos_access_cb_init(_unit);
    sw_state_dpp_bcm_rx_access_cb_init(_unit);
    sw_state_dpp_bcm_stack_access_cb_init(_unit);
    sw_state_dpp_bcm_stg_access_cb_init(_unit);
    sw_state_dpp_bcm_trill_access_cb_init(_unit);
    sw_state_dpp_bcm_trunk_access_cb_init(_unit);
    sw_state_dpp_bcm_vlan_access_cb_init(_unit);
    sw_state_dpp_bcm_vswitch_access_cb_init(_unit);
    sw_state_access[_unit].dpp.bcm.verify = sw_state_dpp_bcm_verify;
    sw_state_access[_unit].dpp.bcm.get_size = sw_state_dpp_bcm_get_size;
    return _SHR_E_NONE;
}
